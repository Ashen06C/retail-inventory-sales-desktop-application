<Window x:Class="RetailApp.Dialogs.ProductEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Product Details" Height="620" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="TxtDialogTitle" Text="Add New Product" FontSize="20" FontWeight="Bold" Foreground="#0D2040"/>
            <TextBlock Text="Enter product details, pricing, and reorder thresholds below." FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Validation error banner -->
        <Border Grid.Row="1" x:Name="BorderError" Background="#FEE2E2" BorderBrush="#EF4444" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,0,12" Visibility="Collapsed">
            <TextBlock x:Name="TxtErrorMessage" Text="Validation Error" Foreground="#B91C1C" FontWeight="SemiBold" TextWrapping="Wrap"/>
        </Border>

        <!-- Form ScrollViewer -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,0,0,16">
            <StackPanel>

                <!-- SKU & Barcode -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Product SKU / Code *" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtSku" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Barcode / EAN (Optional)" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtBarcode" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                    </StackPanel>
                </Grid>

                <!-- Product Name -->
                <StackPanel Margin="0,0,0,14">
                    <TextBlock Text="Product Name *" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtName" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                </StackPanel>

                <!-- Category & Unit -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Category" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <ComboBox x:Name="CmbCategory" Height="38" IsEditable="True" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Unit of Measure" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <ComboBox x:Name="CmbUnit" Height="38" IsEditable="True" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                    </StackPanel>
                </Grid>

                <!-- Cost Price & Selling Price -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Cost Price (Rs) *" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtCostPrice" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Selling Price (Rs) *" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtSellingPrice" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                    </StackPanel>
                </Grid>

                <!-- Current Stock & Reorder Level -->
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="LblStock" Text="Initial Stock Quantity *" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtStock" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                        <TextBlock x:Name="TxtStockHelp" Text="Initial inventory for new product" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Low Stock Reorder Level *" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                        <TextBox x:Name="TxtReorderLevel" Height="38" Padding="8,6" FontSize="14" BorderBrush="#CBD5E1" Background="White"/>
                        <TextBlock Text="Triggers low stock warning" FontSize="11" Foreground="#94A3B8" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>

                <!-- Description -->
                <StackPanel Margin="0,0,0,14">
                    <TextBlock Text="Description / Notes" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,4"/>
                    <TextBox x:Name="TxtDescription" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Padding="8,6" FontSize="13" BorderBrush="#CBD5E1" Background="White"/>
                </StackPanel>

                <!-- Active Status -->
                <CheckBox x:Name="ChkIsActive" Content="Product is active and available for retail sale" IsChecked="True" FontSize="13" FontWeight="SemiBold" Foreground="#0D2040" Margin="0,4,0,0"/>

            </StackPanel>
        </ScrollViewer>

        <!-- Actions -->
        <Border Grid.Row="3" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="0,16,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" Width="100" Height="40" Background="#F1F5F9" Foreground="#475569" BorderThickness="0" FontWeight="SemiBold" Click="BtnCancel_Click" Cursor="Hand" Margin="0,0,12,0"/>
                <Button x:Name="BtnSave" Content="Save Product" Width="130" Height="40" Background="#0D2040" Foreground="White" BorderThickness="0" FontWeight="Bold" Click="BtnSave_Click" Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
