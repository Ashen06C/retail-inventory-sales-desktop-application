<UserControl x:Class="RetailApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="#F8FAFC">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel>

            <!-- Page Header -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Executive Dashboard &amp; Analytics" FontSize="22" FontWeight="Bold" Foreground="#0D2040"/>
                    <TextBlock Text="Live retail performance metrics, stock alerts, and quick POS actions." FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="↻ Refresh Metrics" Style="{StaticResource SecondaryButton}" Command="{Binding RefreshCommand}" Margin="0,0,10,0"/>
                    <Button Content="⬇ Export Inventory CSV" Style="{StaticResource SecondaryButton}" Command="{Binding ExportReportCommand}"/>
                </StackPanel>
            </Grid>

            <!-- KPI Metric Cards Grid -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Card 1: Today's Revenue -->
                <Border Grid.Column="0" Style="{StaticResource KpiCard}" BorderBrush="#E2E8F0">
                    <StackPanel>
                        <TextBlock Text="TODAY'S REVENUE" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                        <TextBlock Text="{Binding TodaySalesTotal, Converter={StaticResource CurrencyConverter}}" FontSize="22" FontWeight="ExtraBold" Foreground="#0D2040" Margin="0,8,0,2"/>
                        <TextBlock Text="{Binding TodayTransactionsCount, StringFormat='{}{0} transactions completed'}" FontSize="12" Foreground="#10B981" FontWeight="SemiBold"/>
                    </StackPanel>
                </Border>

                <!-- Card 2: Items Sold Today -->
                <Border Grid.Column="2" Style="{StaticResource KpiCard}" BorderBrush="#E2E8F0">
                    <StackPanel>
                        <TextBlock Text="ITEMS SOLD TODAY" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                        <TextBlock Text="{Binding TodayItemsSold}" FontSize="22" FontWeight="ExtraBold" Foreground="#0D2040" Margin="0,8,0,2"/>
                        <TextBlock Text="Units dispatched at checkout" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </Border>

                <!-- Card 3: Inventory Value -->
                <Border Grid.Column="4" Style="{StaticResource KpiCard}" BorderBrush="#E2E8F0">
                    <StackPanel>
                        <TextBlock Text="INVENTORY COST VALUE" FontSize="11" FontWeight="Bold" Foreground="#64748B"/>
                        <TextBlock Text="{Binding TotalInventoryCostValue, Converter={StaticResource CurrencyConverter}}" FontSize="22" FontWeight="ExtraBold" Foreground="#0D2040" Margin="0,8,0,2"/>
                        <TextBlock Text="{Binding TotalProductsCount, StringFormat='{}Across {0} active SKUs'}" FontSize="12" Foreground="#64748B"/>
                    </StackPanel>
                </Border>

                <!-- Card 4: Low Stock Alert -->
                <Border Grid.Column="6" Style="{StaticResource KpiCard}" BorderBrush="#FDE68A" Background="#FFFBEB">
                    <StackPanel>
                        <TextBlock Text="REPLENISHMENT ALERTS" FontSize="11" FontWeight="Bold" Foreground="#B45309"/>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,2">
                            <TextBlock Text="{Binding LowStockCount}" FontSize="22" FontWeight="ExtraBold" Foreground="#D97706"/>
                            <TextBlock Text=" Low" FontSize="14" FontWeight="Bold" Foreground="#D97706" VerticalAlignment="Bottom" Margin="2,0,10,2"/>
                            <TextBlock Text="{Binding OutOfStockCount}" FontSize="22" FontWeight="ExtraBold" Foreground="#EF4444"/>
                            <TextBlock Text=" Out" FontSize="14" FontWeight="Bold" Foreground="#EF4444" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                        </StackPanel>
                        <TextBlock Text="Items requiring purchase order" FontSize="12" Foreground="#B45309"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Quick Action Launcher Bar -->
            <Border Style="{StaticResource ModernCard}" Margin="0,0,0,24" Padding="16,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Background="#EEF2FF" CornerRadius="6" Padding="8,6" Margin="0,0,12,0">
                            <TextBlock Text="⚡ Quick Operations" FontWeight="Bold" Foreground="#3730A3" FontSize="13"/>
                        </Border>
                        <TextBlock Text="Instantly access common cashier &amp; storekeeper operations:" VerticalAlignment="Center" Foreground="#64748B" FontSize="13"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <Button Content="🛒 Open POS Sales Screen" Style="{StaticResource EmeraldButton}" Command="{Binding QuickPosCommand}" Margin="0,0,10,0" Padding="16,6"/>
                        <Button Content="📦 Manage Stock &amp; Restock" Style="{StaticResource PrimaryButton}" Command="{Binding QuickStockCommand}" Margin="0,0,10,0" Padding="16,6"/>
                        <Button Content="🏷 Product Catalog" Style="{StaticResource SecondaryButton}" Command="{Binding QuickProductsCommand}" Padding="16,6"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Two-Column Layout: Best Sellers & Stock Alerts -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Left Column: Top 5 Selling Products -->
                <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <TextBlock Text="Top Selling Products" FontSize="16" FontWeight="Bold" Foreground="#0D2040"/>
                        <TextBlock Text="Highest moving products by total sales revenue" FontSize="12" Foreground="#64748B" Margin="0,2,0,12"/>

                        <DataGrid ItemsSource="{Binding TopProducts}" MaxHeight="220" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="90"/>
                                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                                <DataGridTextColumn Header="Units Sold" Binding="{Binding TotalSoldQty}" Width="80"/>
                                <DataGridTextColumn Header="Revenue" Binding="{Binding TotalRevenue, Converter={StaticResource CurrencyConverter}}" Width="110"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Right Column: Urgent Low Stock Alerts -->
                <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Low Stock Warning Alerts" FontSize="16" FontWeight="Bold" Foreground="#0D2040"/>
                                <TextBlock Text="Inventory at or below safety reorder threshold" FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Restock Center ➔" Style="{StaticResource SecondaryButton}" Command="{Binding QuickStockCommand}" Height="30" Padding="10,0"/>
                        </Grid>

                        <DataGrid ItemsSource="{Binding LowStockProducts}" MaxHeight="220" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="SKU" Binding="{Binding Sku}" Width="80"/>
                                <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="*"/>
                                <DataGridTextColumn Header="Stock" Binding="{Binding CurrentStock}" Width="60"/>
                                <DataGridTextColumn Header="Reorder" Binding="{Binding ReorderLevel}" Width="60"/>
                                <DataGridTemplateColumn Header="Status" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border Background="{Binding StockStatus, Converter={StaticResource StockStatusBadgeBgConverter}}" CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                                <TextBlock Text="{Binding StockStatus}" Foreground="{Binding StockStatus, Converter={StaticResource StockStatusColorConverter}}" FontWeight="Bold" FontSize="11"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Recent Sales Feed -->
            <Border Style="{StaticResource ModernCard}">
                <StackPanel>
                    <TextBlock Text="Recent Transactions" FontSize="16" FontWeight="Bold" Foreground="#0D2040"/>
                    <TextBlock Text="Latest completed sales at checkout counter" FontSize="12" Foreground="#64748B" Margin="0,2,0,12"/>

                    <DataGrid ItemsSource="{Binding RecentSales}" MaxHeight="200" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Invoice #" Binding="{Binding InvoiceNumber}" Width="150"/>
                            <DataGridTextColumn Header="Date &amp; Time" Binding="{Binding SaleDate, StringFormat='dd MMM yyyy, hh:mm tt'}" Width="170"/>
                            <DataGridTextColumn Header="Cashier" Binding="{Binding CashierName}" Width="120"/>
                            <DataGridTextColumn Header="Payment" Binding="{Binding PaymentMethod}" Width="100"/>
                            <DataGridTextColumn Header="Grand Total" Binding="{Binding GrandTotal, Converter={StaticResource CurrencyConverter}}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
